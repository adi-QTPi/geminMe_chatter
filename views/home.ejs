<!DOCTYPE html>
<html lang="en" class="h-100">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>home page</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.6/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-4Q6Gf2aSP4eDXB8Miphtr37CMZZQ5oXLH2yaXMJ2w8e2ZtHTl7GptT4jmndRuHDT" crossorigin="anonymous">
    
    <script defer src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.11.8/dist/umd/popper.min.js" integrity="sha384-I7E8VVD/ismYTF4hNIPjVp/Zjvgyol6VFvRkX/vR+Vc4jQkC+hVqc2pM8ODewa9r" crossorigin="anonymous"></script>
    <script defer src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.6/dist/js/bootstrap.min.js" integrity="sha384-RuyvpeZCxMJCqVUGFI0Do1mQrods/hhxYlcVfGPOfQtPJh0JCw12tUAZ/Mv10S7D" crossorigin="anonymous"></script>
    
    <link rel="stylesheet" href="/css/styles.css">
</head>
<nav class="navbar navbar-dark bg-dark d-flex flex-column flex-lg-row align-items-center">
    <div class="col-4 mx-auto ms-lg-0 d-flex align-items-center justify-content-start px-lg-5 p-1">
        <img class="h-2rem" src="/images/logo-compact.png" alt="compact-logo">
        <div class="text-light bitcount-grid-single-200 fs-3">GeminMe</div>
    </div>

    <% if (toHomePage && toHomePage.user) { %>
        <div class="text-light mx-auto d-flex flex-column justify-content-around align-items-center">
            <div class="fs-small text-primary">
                Welcome 
                <span class="fs-medium truculenta-normal"><%= toHomePage.user.displayname %></span>
            </div>
            <div class="fs-smaller text-secondary">(<%= toHomePage.user.emailid %>)</div>
        </div>
    <% } else { %>
        <div class="mx-auto d-flex flex-column justify-content-around align-items-center">
            <div class="text-secondary"><span class="text-primary fs-3">Sign In</span> for Contextual conversations</div>
        </div>
    <% } %>

    <% if (toHomePage && toHomePage.user) { %>
        <form class="ms-auto col-lg-2 me-5 pe-1" action="/logout" method="post">
            <button class="btn btn-danger col-12 bitcount-grid-single-400 fs-3" type="submit">Logout</button>
        </form>
    <% } else { %>
        <form class="ms-auto col-lg-2 me-5 pe-1" action="/login" method="get">
            <button class="btn btn-info col-12 bitcount-grid-single-400 fs-3" type="submit">Signin</button>
        </form>
    <% } %>
</nav>
<body class="bg-secondary h-100 d-flex flex-column">
    <div class="d-flex flex-column align-items-center justify-content-center  gap-3 flex-grow-1">
        <div class="container col-11 col-lg-10 response-space fs-4 overflow-auto text-light mt-4">
            <% if (toHomePage && toHomePage.response) { %>
                <%- toHomePage.response %>
            <% } else { %>
             <div class="fs-2 truculenta-normal">
                Hello user ! 
             </div>
            <% } %>
        </div>

        <form class="form-floating mt-auto mb-5 mx-auto col-10 col-lg-6 d-flex gap-4 position-sticky-bottom" action="/api/ask" method="post" id="form">
            <textarea class="form-control form-control-lg" placeholder="abc" type="text" name="prompt" id="expandable-prompt" autofocus></textarea>
            <label for="prompt" >so geminMe, tell me...</label>
            <button class="btn btn-dark w-auto fs-small truculenta-spaced" type="submit">Submit <br> (cmd+enter)</button>
        </form>
    </div>
</body>
<script src="/js/home.js" defer></script>
<!-- js file path relative to the app.static() whichever file in the argument of it...! -->
</html>